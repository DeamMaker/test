spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/test?userUnicode=true&characterEncode=utf-8
    username: root
    password: 123456

  redis:
    port: 6379
    database: 0
    host: localhost
    password:
    jedis:
      pool:
        max-active: 8
        max-wait: -1
        max-idle: 8
        min-idle: 0
    connect-timeout: 5000